{
  "name": "ftk-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "server": "node server.js",
    "test": "NODE_ENV=test node --test",
    "dev": "concurrently \"npm run server\" \"npm --prefix frontend run dev -- --host 0.0.0.0 --port 4173\"",
    "build": "npm --prefix frontend run build",
    "check": "node --check server.js && node --check move-listener.js && node --check security.js && node --check atomic-rate-gate.js && node --check shared/ftk-api-gate.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "ethers": "^6.17.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "overrides": {
    "body-parser": "1.20.6",
    "shell-quote": "1.10.0"
  }
}
